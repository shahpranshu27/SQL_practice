create database jdbc_db;

use jdbc_db;

create table register(
name varchar(50),
email varchar(50),
password varchar(50),
gender varchar(10),
city varchar(50)
);

desc register;

select * from register;

